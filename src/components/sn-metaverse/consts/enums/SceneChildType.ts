export enum ScenarioType {
  MESH = "Mesh",
}

export enum ScenarioData {
  PHYSICS = "physics",
  SCENARIO = "scenario",
  SPAWN = "spawn",
}

export enum ScenarioDataType {
  BOX = "box",
  TRIMESH = "trimesh",
  CAR = "car",
  CHARACTER = "player",
  COLLISION = "collision",
  SPHERE = "sphere",
  STEERING_WHEEL = "steering_wheel",
  WHEEL = "wheel",
  SEAT = "seat",
  DOOR = "door_object",
}
